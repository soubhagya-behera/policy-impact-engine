# Local-only secrets and environment (NEVER commit).
# Mirrors the application.properties rule: real credentials live only
# in Git-ignored files. Copy .env.example to .env and edit there.
.env
!.env.example
